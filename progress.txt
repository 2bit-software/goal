## Codebase Patterns
- Generated Go is committed under internal/**/*.go and gated by `task check` →
  `verify-generated`: it re-emits to a scratch tree and diffs against the tree.
  Any change to emit output requires re-running `task generate` so the committed
  files match, or the drift gate fails.
- `cmd/goal/main.go` is hand-written Go (NOT goal-generated). Edit it directly,
  then `task build` rebuilds bin/goal before `task generate` regenerates internal.
- The emit write path is `emitFiles` in cmd/goal/main.go; corpus/by-example tests
  compare in-memory pipeline output (gf.Go), not on-disk emitted files, so a
  write-time change (e.g. a header) does not affect those goldens.
- The project verify command is `task check` (vet + build + verify-generated +
  go test ./...). Zero-dependency: stdlib `testing` only, no testify.
- morse-code `history/` is gitignored — initiative artifacts are not committed.
- Emit-lifecycle ownership is keyed on the goal line-1 marker, defined ONCE as
  `goalGeneratedMarker` in cmd/goal/main.go; reuse `IsGoalGenerated` rather than
  re-deriving the marker. Refactoring an emitted literal into a same-valued
  constant keeps `verify-generated` green with zero regen (no internal/ churn).

## [2026-07-09T05:23:31Z] - US-002
- Added the single, version-independent goal-generated ownership predicate
  `IsGoalGenerated(content []byte) bool` in cmd/goal/main.go, keyed only on the
  file's first line equalling the goal line-1 marker. Factored that marker into a
  shared `goalGeneratedMarker` const that `goalGeneratedHeader` now also uses, so
  emit and the matcher share exactly one definition. Ignores line-2 version;
  tolerates a single CRLF `\r`; total function (empty content -> false).
- Added table-driven `TestIsGoalGenerated` (cmd/goal/main_test.go): true for
  `vdev` and `v9.9.9`; false for protoc-gen-go, stringer, headerless, a marker
  below line 1, and empty content.
- Files changed: cmd/goal/main.go, cmd/goal/main_test.go.
- Verified: `task check` exit 0 (drift gate green — the marker literal is
  unchanged, so no re-emit was needed and no internal/**/*.go churned). Commit
  ad32c02.
- Learnings for future iterations:
  - US-005 (prune) and US-006 (clean) must call `IsGoalGenerated` — do not
    re-derive the marker; it lives in `goalGeneratedMarker`.
  - Refactoring an emitted literal into a constant with an identical value keeps
    verify-generated green with zero regen — safe pattern for touching emit code
    without the 76-file header churn US-001 required.
---

## [2026-07-09T05:14:36Z] - US-001
- Made `goal build --emit` write a goal-owned generated-code header on every .go
  file: line 1 `// Code generated by goal; DO NOT EDIT.`, line 2
  `// generator: goal v<version>` (deterministic, version-only). Added
  `goalGeneratedHeader(version)` and prepend it in `emitFiles`.
- Removed the shell header-prepend from the Taskfile `generate` (now a verbatim
  `cp`) and `verify-generated` steps; regenerated all committed internal/**/*.go
  (76 files) to carry the new two-line header. Added `TestEmitWritesGoalHeader`.
- Files changed: cmd/goal/main.go, cmd/goal/main_test.go, Taskfile.yml,
  internal/**/*.go (regenerated).
- Verified: `task check` exit 0 (drift gate green, all tests pass). Commit 6a3b27c.
- Learnings for future iterations:
  - The header line-1 marker is the ownership key US-002's matcher must key on,
    version-independent (line 2 carries the version). Format emitted: `vdev` for
    dev builds; the `v.+` shape tolerates any goreleaser version.
  - When changing emit output, always `task generate` then `task check` — the
    76-file header churn (net +1 line each) is expected and body-clean.
---
