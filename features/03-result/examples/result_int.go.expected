package num

import (
	"errors"
	"strconv"
)

func parsePositive(s string) (ok int, err1 error) {
	n, err := strconv.Atoi(s)
	if err != nil {
		return ok, err
	}
	if n <= 0 {
		return ok, errors.New("not positive")
	}
	return n, nil
}
