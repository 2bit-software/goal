package config

import "errors"

type Config struct {
	Raw string
}

func parse(s string) (ok Config, err error) {
	if s == "" {
		return ok, errors.New("empty input")
	}
	return Config{Raw: s}, nil
}
