package strs

/// repeat concatenates s n times.
/// >>> repeat("ab", 2)
/// "abab"
/// >>> repeat("x", 3)
/// "xxx"
func repeat(s string, n int) string {
	out := ""
	for i := 0; i < n; i++ {
		out += s
	}
	return out
}
