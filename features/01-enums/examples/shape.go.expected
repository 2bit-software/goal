package shape

type Circle struct {
	Radius float64
}

func (Circle) isShape() {}

type Rectangle struct {
	Width  float64
	Height float64
}

func (Rectangle) isShape() {}

type Shape interface{ isShape() }

func unit() Shape {
	return Circle{Radius: 1}
}
