package shape

type Circle struct {
	Radius float64
}
type Rectangle struct {
	Width  float64
	Height float64
}

type Shape interface{ isShape() }

func (Circle) isShape()    {}
func (Rectangle) isShape() {}

func unit() Shape {
	return Circle{Radius: 1}
}
