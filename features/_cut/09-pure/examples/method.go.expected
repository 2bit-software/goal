package geom

type Vec struct {
	x float64
	y float64
}

// Dot is a pure method — the modifier sits before the receiver-bearing `func`,
// and the lowering strips it the same way it strips it from a free function.
func (v Vec) Dot(o Vec) float64 {
	return v.x*o.x + v.y*o.y
}
