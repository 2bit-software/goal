package users

type Role int

const (
	RoleMember Role = iota
	RoleAdmin
)

type User struct {
	name  string
	email string
	role  Role
	admin bool
}

func newAdmin() User {
	return User{name: "root", email: "root@x", role: RoleAdmin, admin: true}
}
