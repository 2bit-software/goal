package users

type ID int

type User struct {
	Name string
}

func find(id ID) *User {
	return nil
}

func exists(id ID) bool {
	if o := find(id); o != nil {
		return true
	} else {
		return false
	}
}
