{
  "project": "goal",
  "branchName": "ralph/ast-frontend-rewrite",
  "description": "Replace the token-splice transpiler with a real lexer/parser/AST front-end, retire the splice passes behind a new Go backend, and move every tool (check, fix, fmt, LSP) onto the shared AST.",
  "verifyCommands": [
    "go build ./...",
    "go vet ./...",
    "go test ./... -count=1"
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Add corpus Case model and loader",
      "description": "As a compiler engineer, I need a runner-independent model of golden cases so the suite is decoupled from package layout.",
      "acceptanceCriteria": [
        "internal/corpus defines Case{ID, Kind(transpile|check|doctest), Input, Expected, Mode(file|package), Normalize} and Load(path)(Manifest,error)",
        "A unit test loads a small fixture manifest from internal/corpus/testdata and asserts every field of one case of each Kind"
      ],
      "priority": 1,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-002",
      "title": "Generate manifest over existing corpus",
      "description": "As a maintainer, I need the existing golden files indexed into one manifest so any runner can consume them.",
      "acceptanceCriteria": [
        "A generator walks features/NN/examples, testdata, and testdata/check and writes corpus/manifest.json without moving any source file",
        "A test asserts the generated manifest contains 51 transpile pairs and 50 check cases (the current corpus counts)"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Counts from audit: 40 feature examples + 11 testdata = 51 transpile pairs; 50 check cases."
    },
    {
      "id": "US-003",
      "title": "Build interface-based transpile runner",
      "description": "As a compiler engineer, I need a transpile runner behind an interface so multiple front-ends can be judged identically.",
      "acceptanceCriteria": [
        "internal/corpus defines Transpiler interface{ Transpile(src string)(pipeline.Output,error) } and a runner that gofmt-normalizes both got and want before comparing",
        "A test runs every transpile case in the manifest against pipeline.Transpile and all pass"
      ],
      "priority": 3,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-004",
      "title": "Build interface-based check runner",
      "description": "As a compiler engineer, I need a check runner that honors inline // want markers so checker cases are runner-independent.",
      "acceptanceCriteria": [
        "Runner defines Checker interface and matches each // want \"substr\" marker against a diagnostic on the same line, failing on any unclaimed Error-severity diagnostic",
        "A test runs every check case in the manifest against check.Analyze and all pass"
      ],
      "priority": 4,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-005",
      "title": "Build doctest sidecar runner",
      "description": "As a compiler engineer, I need doctest cases compared via the manifest so the doctest output is part of the same suite.",
      "acceptanceCriteria": [
        "Runner compares Output.Test to the .go.expected sidecar (gofmt-normalized) for every doctest case in the manifest",
        "A test runs all doctest cases against pipeline.Transpile and all pass"
      ],
      "priority": 5,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-006",
      "title": "Rewire existing harnesses to runner",
      "description": "As a maintainer, I need the old test harnesses to delegate to the corpus runner so hardcoded paths disappear.",
      "acceptanceCriteria": [
        "pipeline_test.go and check_test.go contain no hardcoded feature-dir lists or filepath.Join(\"..\",\"..\",\"features\",...) paths (grep returns nothing)",
        "The full suite remains green after the rewrite"
      ],
      "priority": 6,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-007",
      "title": "Add behavioral tier: compile output",
      "description": "As a compiler engineer, I need generated Go compiled per case so conformance is judged by behavior, not Go spelling.",
      "acceptanceCriteria": [
        "For every transpile case the runner writes Output.Go to a temp module and runs go build + go vet on it",
        "A test asserts every transpile case's generated Go builds and vets cleanly"
      ],
      "priority": 7,
      "passes": true,
      "notes": "Reuse the temp-dir machinery from goal build. This tier is implementation-independent and gates all later phases."
    },
    {
      "id": "US-008",
      "title": "Add behavioral tier: run doctests",
      "description": "As a compiler engineer, I need doctest sidecars executed so behavior is proven, not just compiled.",
      "acceptanceCriteria": [
        "For each case that has a doctest sidecar, the runner writes both files to a temp module and runs go test on it",
        "A test asserts every doctest-bearing case passes go test in its temp module"
      ],
      "priority": 8,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-009",
      "title": "Reify package-mode tests as fixtures",
      "description": "As a maintainer, I need the inline package-mode tests as on-disk fixtures so they live in the manifest.",
      "acceptanceCriteria": [
        "foreign_test.go and pipeline_package_test.go inline sources become on-disk multi-file fixtures with a declared import map, indexed as Mode=package cases",
        "The runner executes the package-mode cases and they pass"
      ],
      "priority": 9,
      "passes": true,
      "notes": "Fixtures under testdata/package/<name>/ with a pkg.json (name + import map). Generate indexes them as Kind=transpile, Mode=package via Case.Package (PackageSpec). corpus.RunPackage transpiles + validates Go + builds each package (foreign import wired into a temp module). Manifest now 107 cases (51+50+4+2)."
    },
    {
      "id": "US-010",
      "title": "Archive legacy per-feature transpilers",
      "description": "As a maintainer, I need the superseded standalone transpilers archived so they stop implying they are live.",
      "acceptanceCriteria": [
        "features/NN/transpiler directories are moved under attic/ and no features/*/transpiler path remains",
        "go build ./... and go test ./... remain green (these were separate modules excluded from the root module)"
      ],
      "priority": 10,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-011",
      "title": "Add token package with positions",
      "description": "As a compiler engineer, I need a token type with line/col positions so the AST can carry real source positions.",
      "acceptanceCriteria": [
        "internal/token defines Kind constants for every goal lexeme and Pos{Offset,Line,Col}",
        "A unit test asserts Kind/String round-trips and Pos ordering"
      ],
      "priority": 11,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-012",
      "title": "Lex core Go tokens with positions",
      "description": "As a compiler engineer, I need a lexer for the Go subset so ordinary source tokenizes with correct positions.",
      "acceptanceCriteria": [
        "internal/lexer tokenizes identifiers, literals, operators, and delimiters with accurate Offset/Line/Col",
        "A test tokenizes a multi-line sample and asserts the kind and position of each token"
      ],
      "priority": 12,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-013",
      "title": "Lex goal-specific lexemes and trivia",
      "description": "As a compiler engineer, I need the lexer to emit goal lexemes so the parser need not fake them.",
      "acceptanceCriteria": [
        "Lexer emits single tokens for ? , => , and ... ; retains /// doc-comment content as trivia; and lexes implements/sealed/from/derive as identifiers",
        "A test asserts each of these lexemes produces exactly one token of the expected kind and that comments attach as trivia"
      ],
      "priority": 13,
      "passes": true,
      "notes": "=> must be one token, not '=' then '>' as the splice passes treat it."
    },
    {
      "id": "US-014",
      "title": "Define core AST nodes and Walk",
      "description": "As a compiler engineer, I need AST node types for the Go subset plus a visitor so tools can traverse one tree.",
      "acceptanceCriteria": [
        "internal/ast defines File, the Go decl/stmt/expr/type nodes goal uses, and Walk(Visitor,Node)",
        "A test builds a tree by hand and asserts Walk visits every node exactly once"
      ],
      "priority": 14,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-015",
      "title": "Add goal declaration AST nodes",
      "description": "As a compiler engineer, I need AST nodes for goal declarations so enums and friends are first-class.",
      "acceptanceCriteria": [
        "ast defines EnumDecl/Variant/PayloadField, SealedInterfaceDecl, ImplementsClause, and from/derive FuncDecl modifiers",
        "A test asserts Walk descends into each new node's children"
      ],
      "priority": 15,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-016",
      "title": "Add goal expression and pattern nodes",
      "description": "As a compiler engineer, I need AST nodes for match, ? and construction so the three meanings of Enum.Variant(x) are distinct node types.",
      "acceptanceCriteria": [
        "ast defines MatchExpr/MatchArm, VariantPattern/RestPattern, UnwrapExpr, VariantLit/LabeledArg, and SpreadElement",
        "A test asserts a construction VariantLit and a destructuring VariantPattern are distinct node types and both walk correctly"
      ],
      "priority": 16,
      "passes": true,
      "notes": "This is the structural fix for the Match-before-Enums ordering hack."
    },
    {
      "id": "US-017",
      "title": "Parse package, imports, declarations",
      "description": "As a compiler engineer, I need top-level declaration parsing so a file's structure becomes an AST.",
      "acceptanceCriteria": [
        "internal/parser parses package clause, imports, and func/type/var/const declarations of the Go subset into ast.File",
        "A test parses a sample file and asserts the declaration list shape"
      ],
      "priority": 17,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-018",
      "title": "Parse statements",
      "description": "As a compiler engineer, I need statement parsing so function bodies become AST.",
      "acceptanceCriteria": [
        "Parser handles assignment, short-var, if/for/switch/return/defer/go and block statements",
        "A test parses a function body with each statement form and asserts the statement list"
      ],
      "priority": 18,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-019",
      "title": "Parse expressions with Pratt and postfix ?",
      "description": "As a compiler engineer, I need precedence-correct expression parsing so postfix ? falls out of the grammar.",
      "acceptanceCriteria": [
        "Parser parses binary/unary/selector/call/index expressions with correct precedence and postfix ? as UnwrapExpr",
        "A test asserts f(x)? , a.b? , and a mixed-precedence binary expression parse to the expected tree shape"
      ],
      "priority": 19,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-020",
      "title": "Parse enum, sealed, implements",
      "description": "As a compiler engineer, I need enum-family parsing so closed types become AST.",
      "acceptanceCriteria": [
        "Parser parses enum declarations (data-less and payload variants), sealed interface, and struct implements clauses",
        "A test parses the 01-enums and 07-implements example inputs and asserts variant/field/implements structure"
      ],
      "priority": 20,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-021",
      "title": "Parse match and patterns",
      "description": "As a compiler engineer, I need match parsing in both positions so value-position match is representable.",
      "acceptanceCriteria": [
        "Parser parses match as both a statement and an expression (var x = match ...), including binding and rest patterns",
        "A test asserts a statement-position and a value-position match both parse with the expected arms"
      ],
      "priority": 21,
      "passes": true,
      "notes": "Value-position match is refused by the current splice approach; it must parse here."
    },
    {
      "id": "US-022",
      "title": "Parse construction, labeled args, spread",
      "description": "As a compiler engineer, I need construction parsing so Enum.Variant(field: v) and ...defaults become AST.",
      "acceptanceCriteria": [
        "Parser parses VariantLit with LabeledArg arguments and SpreadElement (...defaults, ...derive(s)) inside composite literals",
        "A test parses Status.Active(since: now()) and a literal containing ...defaults and asserts the nodes"
      ],
      "priority": 22,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-023",
      "title": "Parse from/derive, assert, doctests",
      "description": "As a compiler engineer, I need the remaining goal constructs parsed so the grammar is complete.",
      "acceptanceCriteria": [
        "Parser parses from func and derive func (bodyless and bodied), assert statements, and /// doctest comments as structured nodes",
        "A test parses the 10-assert, 11-doctests, and 12-derive-convert example inputs and asserts their nodes"
      ],
      "priority": 23,
      "passes": true,
      "notes": "Parser now parses from/derive func modifiers (reusing FuncDecl.Mod/ModPos + optional body), assert (bare + printf-message, top-level-comma split), and /// doctests as structured nodes (DocComment/Doctest attached to FuncDecl.Doc). New ast.AssertStmt. DOC_COMMENT tokens are retained in the stream (not stripped) and collected before each decl."
    },
    {
      "id": "US-024",
      "title": "Gate: parse 100% of corpus",
      "description": "As a compiler engineer, I need every corpus input to parse so the parser is proven complete against the spec.",
      "acceptanceCriteria": [
        "A test iterates every .goal input in the corpus manifest and parses it with zero errors",
        "The test fails loudly listing any input that does not parse"
      ],
      "priority": 24,
      "passes": true,
      "notes": "Parser now parses 100% of the corpus (104 unique .goal inputs across 107 manifest cases). Closed 4 grammar gaps: multi-element type-arg/index lists (new ast.IndexListExpr; single index keeps IndexExpr); type-literal operands in expression position ([]byte(p) conversions, map[K]V{}/[]T{} composites) via parseOperand+compositeOK; optional-colon enum payload fields (Active { since int }); brace-less statement match-arm bodies (=> return true) via parseMatchArm dispatching parseStmt on statement keywords. Gate test: corpus.TestParseGate enumerates every unique input and lists any that fail."
    },
    {
      "id": "US-025",
      "title": "Add AST snapshot tests",
      "description": "As a compiler engineer, I need stable AST snapshots so parser structure is pinned per construct.",
      "acceptanceCriteria": [
        "Parser can render an AST to a deterministic textual form (e.g. s-expression) and snapshot goldens exist for one input per goal construct",
        "A test compares the rendered AST of each representative input to its checked-in snapshot"
      ],
      "priority": 25,
      "passes": true,
      "notes": "Replaces the tree-sitter differential as the loop-ready gate; grammar.js remains a manual cross-check reference. ast.Sexpr (internal/ast/dump.go) renders a position-free, deterministic s-expression via reflection (skips token.Pos, names token.Kind/FuncMod/ChanDir, elides zero fields); internal/parser TestASTSnapshots parses one representative input per goal construct and diffs against testdata/snapshots/*.sexpr (-update-snapshots regenerates)."
    },
    {
      "id": "US-026",
      "title": "Add Backend and Formatter interfaces",
      "description": "As a compiler engineer, I need pluggable backend/formatter seams so the new engine can run behind a flag.",
      "acceptanceCriteria": [
        "Backend interface{ Emit(*ast.File,*sema.Info)(Output,error) } and Formatter interface exist; the driver selects engine via a flag (e.g. --engine=ast)",
        "A test transpiles a goal file that uses no goal-specific constructs through the new engine and the output compiles via the behavioral tier"
      ],
      "priority": 26,
      "passes": true,
      "notes": "Phase 2 seam. internal/sema (placeholder Info+New, filled by US-027); internal/backend defines Backend{Emit(*ast.File,*sema.Info)(pipeline.Output,error)} + Formatter{Format([]byte)([]byte,error)} + GoFormatter (go/format) + goBackend (minimal plain-Go emitter, emit.go) + Transpile engine (parse->sema->Emit->format, satisfies corpus.Transpiler). Driver --engine=splice|ast flag in parseFlags; ast routes per-file via backend.Transpile (transpilePackageAST); splice unchanged + default. AC2 proven by corpus.RunCompile over testdata/plain.goal. Emitter errors on goal-specific/unsupported nodes (US-032+)."
    },
    {
      "id": "US-027",
      "title": "Resolve symbols by AST walk",
      "description": "As a compiler engineer, I need name-keyed facts derived from the AST so analyze's token scanning is retired.",
      "acceptanceCriteria": [
        "internal/sema builds enums/structs/signatures/from-registry/methods by walking the AST",
        "A test asserts sema resolves the same symbols as analyze.Build for a set of representative inputs, including a struct field whose type contains an embedded comma (the analyze comma-split bug)"
      ],
      "priority": 27,
      "passes": true,
      "notes": "Fixes the known analyze.parseStructBody comma-split bug structurally."
    },
    {
      "id": "US-028",
      "title": "Wrap go/types depth checks behind interface",
      "description": "As a compiler engineer, I need the go/types crutch behind a TypeChecker interface so it can be swapped later without caller changes.",
      "acceptanceCriteria": [
        "TypeChecker interface is defined and the existing typecheck (transpile then go/types) implements it",
        "A test exercises the depth checks through the interface and the existing typecheck cases still pass"
      ],
      "priority": 28,
      "passes": true,
      "notes": "Decision 4: no native goal type checker until the runtime forces it. internal/typecheck now defines TypeChecker{Check(pkg *project.Package)([]Diagnostic,error)} keyed on diagnostics (NOT Load's *types.Package, so a native checker with no go/types view can satisfy it); GoTypesChecker is the transpile-then-go/types crutch (Load + CheckImplements/CheckMustUse/CheckNoZeroValue). cmd/goal runDepthChecks routes through the interface value, so the implementation can flip with no caller change. Load + Check* stay exported and unchanged."
    },
    {
      "id": "US-029",
      "title": "Reimplement exhaustiveness check on AST",
      "description": "As a goal developer, I need exhaustiveness checking over the AST so match diagnostics survive the front-end change.",
      "acceptanceCriteria": [
        "sema implements the match exhaustiveness check over the AST",
        "Every exhaustiveness-related case in testdata/check passes through the sema checker via the corpus runner"
      ],
      "priority": 29,
      "passes": true,
      "notes": "internal/sema/check.go adds the AST exhaustiveness check (CheckExhaustive + the Check aggregator US-030/031 extend) plus sema's own Diagnostic/Severity spine, keeping sema independent of analyze/check. Enum resolved from the first variant-qualified arm (position-independent); Result/Option skipped; unknown enum -> Warning deferral; missing variants w/o `_` -> Error, message wording matched to the legacy check so the inline // want markers pass. corpus/sema_checker.go (SemaCheck) parses->Resolve->sema.Check->converts to check.Diagnostic via Pos.Offset, satisfying corpus.Checker; corpus.TestSemaExhaustiveRunner drives the 7 testdata/check/02-match cases through RunCheck and all pass."
    },
    {
      "id": "US-030",
      "title": "Reimplement field-completeness check on AST",
      "description": "As a goal developer, I need no-zero-value (section 8) checking over the AST so field diagnostics survive.",
      "acceptanceCriteria": [
        "sema implements the field-completeness check over the AST (deferrals as Warnings, violations as Errors)",
        "Every field-completeness case in testdata/check passes through the sema checker via the corpus runner"
      ],
      "priority": 30,
      "passes": true,
      "notes": "internal/sema/fields.go adds CheckFields (struct-literal + variant-construction §8 completeness) wired into sema.Check. Correct by construction on the AST: a struct literal is *ast.CompositeLit (Ident type), a construction is *ast.VariantLit (parser builds it only for labeled-arg calls), and a match-arm binding is *ast.VariantPattern — so walking VariantLit alone cannot mis-read a binding as a construction (the legacy match_binding false positive is impossible). Struct tags ride on the AST field; non-Ident composite types (slice/map) are skipped. ...defaults/...derive SpreadElement => complete-by-construction; unknown keyed literal type => Warning deferral; omission => Error. Messages mirror internal/check/fields.go verbatim for // want parity. corpus.TestSemaFieldsRunner drives all 9 testdata/check/08-no-zero-value cases through SemaCheck via RunCheck; all pass."
    },
    {
      "id": "US-031",
      "title": "Reimplement mustuse/implements/question checks",
      "description": "As a goal developer, I need the remaining lexical checks over the AST so all diagnostics survive.",
      "acceptanceCriteria": [
        "sema implements must-use, implements, and ?-arity/refusal checks over the AST",
        "Every must-use, implements, and question case in testdata/check passes through the sema checker via the corpus runner"
      ],
      "priority": 31,
      "passes": true,
      "notes": "Ported must-use (03), implements (07), and the ?-checks (05 open-E CheckQuestion + 06 closed-E CheckClosed) onto the AST in internal/sema, wired into sema.Check. Correct by construction: a dropped Result is ExprStmt{X:CallExpr}, a consumed `f()?` is ExprStmt{X:UnwrapExpr} (X is the UnwrapExpr, not the call), so the drop walk never misreads a propagation; a match-arm Result.Err(b) is a VariantPattern, a constructed Result.Err(E.V) is CallExpr{Fun:SelectorExpr}, so the closedness walk never misreads a binding (legacy false positives structurally impossible). Extended Info with Interfaces/EmbeddedIfaces (resolveInterface) so implements folds embedded interfaces; interface method Sig uses the same paramTypeListFL/joinTypes normalization as concrete methods, Raw left empty so the missing-method suggestion renders `Name() results`. ?-sites are read only at well-formed statement positions (AssignStmt RHS / ExprStmt X). Messages mirror internal/check/{mustuse,implements,question,closed}.go verbatim for // want parity. corpus.TestSemaQuestionImplementsRunner drives all 03-result/06-error-e/07-implements cases (21) through SemaCheck via RunCheck; all pass. sema.Check now runs the new checks for the 02/08 runners too (verified no false Error)."
    },
    {
      "id": "US-032",
      "title": "Emit the Go subset from AST",
      "description": "As a compiler engineer, I need a code emitter for ordinary Go so the backend can print non-goal code.",
      "acceptanceCriteria": [
        "backend/go walks the AST and emits Go source text for the Go subset, formatted once via Formatter",
        "A goal file using only plain-Go constructs transpiles through the new backend and passes the behavioral tier"
      ],
      "priority": 32,
      "passes": true,
      "notes": "internal/backend/emit.go now covers the full ordinary-Go subset the parser produces: added *ast.SwitchStmt/*ast.CaseClause emission (switchStmt/caseClause: optional init + tag, case e1,e2:/default: clauses) — the US-026 seed emitter omitted them. Also fixed a latent US-026 bug: fieldList joined struct fields / interface methods with COMMAS (invalid Go gofmt can't repair) — replaced the StructType/InterfaceType expr cases with newline-separating structType/interfaceType emitters; interface methods now render `Name(params) results` not `Name func(...)`. AC2 proven by corpus.RunCompile (temp-module go build+vet) over new testdata/plain_full.goal (switch, struct+composite, interface, map+range, slice, defer, multi-return, const/var). Func-literal IIFEs and trailing variadic call spread are NOT in goal's parsed subset (parser rejects them) — out of scope. Format-once discipline unchanged (GoFormatter). Goal-specific nodes still return descriptive unsupported errors (US-033+)."
    },
    {
      "id": "US-033",
      "title": "Lower and emit enums and implements",
      "description": "As a goal developer, I need enum/sealed/implements lowering on the new backend so closed types work.",
      "acceptanceCriteria": [
        "lower+backend produce the sum encoding for enums and the assertion/marker for implements",
        "The 01-enums and 07-implements transpile cases pass the behavioral tier through the new backend"
      ],
      "priority": 33,
      "passes": true,
      "notes": "Folded goal-construct lowering into internal/backend (no separate lower pkg; arch doc marks lower/ir optional). New internal/backend/lower.go holds the encoders mirrored from internal/pass (genEnum/genSealedInterface/genMarkerMethod/exported/pointerReceiverSet + nil-safe enumOf/isSealed over sema.Info). backend.Transpile now calls sema.Resolve(file) (was sema.New()) so the emitter has Enums/Sealed facts; goBackend.Emit threads info into emitFile(file, info). emit.go: decl() dispatches EnumDecl->genEnum and SealedInterfaceDecl->genSealedInterface; structType drops the implements clause (emits plain struct); implementsMarkers emits the §8.5 marker AFTER the type decl (sealed->func (T) isI(){}; else pointerRecv->var _ I=(*T)(nil); else var _ I=T{}); selectorExpr lowers a data-less enum variant (Status.Pending->Status(Status_Pending{}), guarded by info.Enums membership so io.Writer/c.n are untouched); variantLit lowers payload construction (Status.Active(since: now())->Status(Status_Active{Since: now()}), labels exported, values recursive so nested constructions lower for free). AC2: TestASTEngineEnumsImplementsBehavioralTier runs all 7 (4 01-enums + 3 07-implements) cases through backend.Transpile + corpus.RunCompile (build+vet). Output matches goldens modulo `type Time int64` vs golden `type Time = int64` alias (pre-existing US-032 TypeSpec.Assign gap; both build+vet; exact parity is US-042)."
    },
    {
      "id": "US-034",
      "title": "Lower and emit Result and Option",
      "description": "As a goal developer, I need open-E Result and Option lowering on the new backend so the core types work.",
      "acceptanceCriteria": [
        "lower+backend produce (T,error) for open-E Result and *T for Option, including statement-position match over them",
        "The 03-result and 04-option transpile cases pass the behavioral tier through the new backend"
      ],
      "priority": 34,
      "passes": true,
      "notes": "AST backend lowers open-E Result[T,error]->native named (__goal_ok T, __goal_err error) returns and Option[T]->*T in internal/backend (emit.go dispatch + lower.go helpers; no separate lower pkg). return Result.Ok(X)->`X, nil`, Result.Err(X)->`__goal_ok, X`; return Option.None->nil, Option.Some(id)->&id else box `__goal_some := x; return &__goal_some`. Statement-position match intercepted in ExprStmt: Result->`lhs, __goal_err := scrut; if __goal_err != nil {errBody} else {okBody}` (Ok binding renamed to __goal_v via emitter.renames, _ when unused; Err binding renamed to __goal_err); Option->`if __goal_o := scrut; __goal_o != nil { [bind := *__goal_o] someBody } else { noneBody }`. fnKind on the emitter scopes constructor lowering; closed-E Result match guarded via sema FuncSignatures mode (US-037). Output is byte-identical to the checked-in 03/04 goldens. AC2: TestASTEngineResultOptionBehavioralTier runs all 8 03-result/04-option cases through backend.Transpile + corpus.RunCompile (build+vet)."
    },
    {
      "id": "US-035",
      "title": "Lower and emit ? with real gensym",
      "description": "As a compiler engineer, I need ? propagation lowered with proper gensym so the magic __goal_ prefix is retired.",
      "acceptanceCriteria": [
        "? propagation is lowered using scope-aware generated identifiers, not the literal __goal_ prefix",
        "The 05-question-prop cases pass the behavioral tier and a test asserts the generated Go contains no __goal_ substring"
      ],
      "priority": 35,
      "passes": true,
      "notes": "AST backend now lowers postfix `?` (ast.UnwrapExpr) at statement position in internal/backend/emit.go (unwrap/unwrapResult/unwrapOption + calleeSig), wired into stmt()'s AssignStmt (name:=expr? / _:=expr?) and ExprStmt (bare expr?) cases. The whole __goal_ gensym family is retired in favor of a scope-aware allocator: emitFile seeds emitter.fileIdents via fileIdentSet(f) (lower.go); funcDecl opens a per-function scope (newScope copy) and mints the open-E Result success/error returns up front (e.gensym(\"ok\"/\"err\")) so the signature and body agree; match/Option-box temps are local gensyms (v/err/o/some) since a statement-position match may sit in a non-Result function (e.g. handle). gensym(want) appends the least integer suffix that avoids any in-scope name — so result_int's user `err` forces the named error return to `err1` (scope-aware witness). Result `?` destructures the callee's lowered arity via sema.FuncSig (plain-error callee -> 1 value, Result callee -> 2); Option `?` mints a fresh pointer temp per site (o, o1) and returns nil. Updated US-034's TestASTEngineResultOptionEncoding to the new names. AC tests: TestASTEngineQuestionBehavioralTier (build+vet over all 5 05-question-prop cases), TestASTEngineQuestionNoMagicPrefix (no __goal_), TestASTEngineQuestionScopeAware (err1 witness), TestASTEngineQuestionEncoding (shapes). Closed-E `?` still deferred to US-037 (closed-E Result functions are roNone, so a `?` there hits the descriptive `?` outside Result/Option error)."
    },
    {
      "id": "US-036",
      "title": "Lower and emit match including value position",
      "description": "As a goal developer, I need full match lowering so value-position match finally works.",
      "acceptanceCriteria": [
        "lower+backend emit a type-switch for enum match in both statement and value position",
        "The 02-match cases plus a new value-position-match corpus case pass the behavioral tier through the new backend"
      ],
      "priority": 36,
      "passes": true,
      "notes": "AST backend lowers enum `match` to a Go type-switch over the §8.1 encoding in statement AND value position (internal/backend/emit.go enumMatch + matchPos posStmt/posReturn/posVar). Dispatch: matchStmt default branch (statement), returnStmt (`return match`), new tryVarMatch off the DeclStmt case (`var name T = match` -> emit `var name T` then the switch). usesBinding mints a scope-aware guard gensym `v` only when some arm references its payload binding; emitEnumArm renames the binding to the guard (existing renames map) and sets armBinding/armFields so a field access on the binding exports (`a.since`->`v.Since`) via a new selectorExpr branch. `_` rest arm -> real `default:`; else panicking default with the §8.2 non-exhaustive message. Mirrors internal/pass/match.go but reads parsed arms + sema.Enum.FieldSet (no token scan). New corpus case features/02-match/examples/shape_value.goal (+.go.expected, golden from the canonical splice engine; manifest regenerated 107->108; generate_test 51->52). AC tests: TestASTEngineEnumMatchBehavioralTier (build+vet over the 4 02-match + shape_value) and TestASTEngineEnumMatchEncoding. Output matches goldens modulo gensym name (`v` vs `__goal_v`, US-035) and the pre-existing `type Time int64` vs `= int64` alias gap (US-042); both build+vet clean."
    },
    {
      "id": "US-037",
      "title": "Lower and emit closed-E Result",
      "description": "As a goal developer, I need closed-E Result lowering so sum-encoded errors work on the new backend.",
      "acceptanceCriteria": [
        "lower+backend emit the Ok/Err sum constructors, closed match, closed ?, and the From-conversion, with the prelude emitted once",
        "The 06-error-e cases pass the behavioral tier through the new backend"
      ],
      "priority": 37,
      "passes": true,
      "notes": "AST backend lowers closed-E Result[T,E] (E!=error, feature 06) in internal/backend (emit.go dispatch + lower.go facts; no separate lower pkg). lower.go: roResultClosed roKind, resultPrelude (the generic Ok/Err sum, mirrored from internal/pass.ResultPreamble), needsResultPrelude(info). emit.go: file() emits the prelude once after import decls (isImportDecl guard) when any func is closed-E; emitter gains closedT/closedE; funcDecl sets fnKind=roResultClosed + closedT/closedE from sema.FuncSig[name] (Mode==ModeResultClosed) and now allows ast.FuncFrom (emits `from func` as a plain Go func — its src->tgt registration lives in sema.FromRegistry, no syntactic residue; FuncDerive still fails, US-039). returnStmt roResultClosed -> emitClosedResultReturn (Result.Ok/Err(X) -> return Ok/Err[closedT,closedE]{Value: X}, X recursive so a nested ParseError.Empty lowers for free). unwrap roResultClosed -> unwrapClosed (`var name T` + type-switch over Ok/Err[calleeT,calleeE]; Err arm returns Err[closedT,closedE]{Value: errValue} where errValue is guard.Value, or conv(guard.Value) via FromRegistry[[2]string{callee.E, caller.E}] when error types differ — fails loudly if the from func is absent). resultMatch routes calleeMode==ModeResultClosed -> closedResultMatch (type-switch on Ok/Err with `binding := guard.Value` per used arm, guard minted only when some arm uses its binding, panicking §8.2 default). Reads T/E + the from-registry off sema.Info, not token scans (mirrors internal/pass/closed.go shapes). AC tests: TestASTEngineClosedResultBehavioralTier (build+vet via corpus.RunCompile over the 3 06-error-e cases) + TestASTEngineClosedResultEncoding (prelude/sum-ctor/closed-match/closed-?/from-conv shapes). Output matches the goldens modulo the scope-aware gensym (`r` vs `__goal_e`, US-035) — exact parity is US-042."
    },
    {
      "id": "US-038",
      "title": "Lower and emit defaults and assert",
      "description": "As a goal developer, I need ...defaults and assert lowering so those constructs work on the new backend.",
      "acceptanceCriteria": [
        "lower+backend expand ...defaults to explicit zero values and assert to an if-panic guard",
        "The 08-no-zero-value and 10-assert cases pass the behavioral tier through the new backend"
      ],
      "priority": 38,
      "passes": true,
      "notes": "AST backend lowers `...defaults` (feature 08, §8.5) and `assert` (feature 10, §4.3/§8.6) in internal/backend (emit.go dispatch + lower.go helpers; no separate lower pkg). `...defaults` (parsed as *ast.SpreadElement{X: Ident\"defaults\"} in a *ast.CompositeLit) expands via compositeLit/defaultEntries: for each declared struct field (sema.Info.Structs[name]) not already keyed, append `name: zeroLit(type)`; a field whose zero is unsafe (nil pointer/map/chan/func, sum type, method-iface) is a located e.fail, mirroring internal/pass.zeroSafety. zeroLit/zeroSafety/baseType mirrored into lower.go reading a `typeDecls` map (built by emitter.buildTypeDecls walking File TypeSpecs: struct->\"struct\", interface->\"interface\", alias->exprText(underlying)) + sema enum/sealed sets — the AST-native replacement for analyze.Tables.TypeDecls. `assert` (*ast.AssertStmt) lowers in stmt() via assertStmt to `if !(cond) { panic(\"assertion failed: <condtext>\"[ + fmt.Sprintf(msg, args...)]) }`; condtext is exprText(Cond) (a fresh sub-emitter, NOT a Builder-copy swap). file() injects `import \"fmt\"` once after the package clause when needsFmtImport(f) (any AssertStmt with Msg) && !importsPkg(f,\"fmt\"). AC2: TestASTEngineDefaultsAssertBehavioralTier runs all 6 (3 08-no-zero-value + 3 10-assert) cases through backend.Transpile + corpus.RunCompile (build+vet). Output matches goldens modulo dropped `//` comments and the `type Name = string` alias gap (US-042); both build+vet clean."
    },
    {
      "id": "US-039",
      "title": "Lower and emit derive/from",
      "description": "As a goal developer, I need derive/from lowering so type-directed conversion works on the new backend.",
      "acceptanceCriteria": [
        "lower+backend expand derive func field-by-field using resolved types (not string parsing) and strip the from modifier",
        "The 12-derive-convert cases pass the behavioral tier through the new backend"
      ],
      "priority": 39,
      "passes": true,
      "notes": "AST backend lowers `derive func` (feature 12, §12) field-by-field in internal/backend (emit.go dispatch + lower.go helpers; no separate lower pkg). funcDecl's d.Mod switch routes ast.FuncDerive -> deriveDecl (FuncFrom already strips to a plain func, US-037). deriveDecl reads source name+type (first param), target type+fallibility (deriveTarget over the result list: single result total, (T,error) fallible), and overrides from a bodied derive (deriveOverrides walks Body->ReturnStmt->CompositeLit: KeyValueExpr override, value Ident `_` => skip; the `...derive(src)` SpreadElement is the implicit fill). genConversion emits `func name(src S) <ret> { var out T; <overrides>; <fields>; return out[, nil] }`; resolveField mirrors internal/pass.resolveField over sema.Info.Structs/FromRegistry (identity src==tgt; registry total `out.F=conv(src.F)`; registry fallible `v,err:=conv(...); if err!=nil {return out,err}; out.F=v`; ptr/Option `*A->*B`; slice `[]A->[]B` make+indexed loop; array `[N]A->[N]B`; map; nested in-file struct recursion via deriveBody). Reads resolved fields/registry, NOT source text (FR-6); an unsourced/unconvertible field is a located e.fail, never a silent zero. Temp names are scope-aware gensyms (`v`/`v1`/`p`/`i`/`s`) not `__goal_v0` (US-035), and one shared error name per fallible derive. typeExprString renders source/target type exprs to sema-canonical keys. AC: TestASTEngineDeriveBehavioralTier runs all 3 12-derive-convert cases (slice/from_storage/to_storage) through backend.Transpile + corpus.RunCompile (build+vet); TestASTEngineDeriveEncoding pins the from-strip/identity/total-leaf/fallible/slice shapes + the `_`-skip absence. Output matches goldens modulo gensym names + dropped `//` comments (US-042). Foreign-package derive (US-009) stays on the splice engine via corpus.RunPackage — out of scope here."
    },
    {
      "id": "US-040",
      "title": "Emit doctest sidecars on new path",
      "description": "As a goal developer, I need doctests extracted and emitted by the new backend so executable docs survive.",
      "acceptanceCriteria": [
        "The new backend extracts /// doctests and emits the _test.go sidecar lowered through the same path",
        "The 11-doctests cases pass the doctest tier through the new backend"
      ],
      "priority": 40,
      "passes": true,
      "notes": "AST backend now emits the doctest `_test.go` sidecar (internal/backend/doctest.go: renderDoctests + emitDoctests), wired into goBackend.Emit so backend.Transpile populates Output.Test (Transpile already gofmt-formats it). Extraction is AST-native: it reads the structured ast.FuncDecl.Doc.Doctests the parser attached in US-023 (no re-lexing), renders the same goal-shaped TestDoctest_<fn>_<n> sidecar shape as internal/pass.RenderDoctests, then lowers it through the SAME emit path by parsing the rendered source back to an *ast.File and calling emitFile with the ORIGINAL file's *sema.Info — so a doctest body's (nested) variant construction / keyed literal lowers to the §8.1 sum encoding exactly as the documented function body does (the emitter's variantLit/selectorExpr read info.Enums by name; the original info, not sema.Resolve(testFile), is what carries those facts). AC tests in internal/backend/backend_test.go: TestASTEngineDoctestTier (sidecar vs golden, gofmt-normalized) + TestASTEngineDoctestExecTier (behavioral: go test in a temp module) over all 4 features/11-doctests cases, plus TestASTEngineDoctestEnumLowering pinning the enum case's nested construction lowering. Output matches the checked-in 11-doctests goldens byte-for-byte (doctest bodies use no gensym temporaries, so the US-035 naming divergence does not apply here)."
    },
    {
      "id": "US-041",
      "title": "Gate: full behavioral parity",
      "description": "As a maintainer, I need the entire corpus green through the new backend so the flip is safe.",
      "acceptanceCriteria": [
        "Every transpile, check, and doctest case passes the behavioral conformance tier through the new (AST) engine",
        "A single test runs the whole manifest under the new engine and reports zero failures"
      ],
      "priority": 41,
      "passes": true,
      "notes": "Closed the 3 under-scoped gaps then made the whole-corpus behavioral gate green on the AST engine. Gap 1 (value-position `:=` match): backend/emit.go now lowers a short-var/assignment `name := match` over an enum (tryAssignMatch + inferMatchType/armBodyType mirror internal/pass.inferMatchType — arms must all be one string lit, one bool lit, or constructions of one enum) to `var name T` + the existing posVar value-position type-switch. Gap 2 (assert-static + derive diagnostics on the AST): new internal/sema/assert.go (CheckAssert: constant-fold a bare bool literal or an int-literal comparison; always-false Error, always-true Warning) and internal/sema/convert.go (CheckConvert: derive-convert totality over file.Decls FuncDerive + Info.Structs/FromRegistry — unsourced/unbridged/fallible-in-total Errors, deferral Warnings), both wired into sema.Check; messages mirror internal/check/{assert,convert}.go verbatim for // want parity. Gap 3 (AST package mode): internal/sema.ResolvePackage+Merge merge name-keyed facts across files (analyze.Merge analogue); internal/backend/package.go TranspilePackage emits each file with the inline closed-E prelude suppressed (new emitter.suppressPrelude / emitFileWith) and appends one shared goal_prelude.go, and enrichForeign reuses analyze.EnrichForeign (the IO seam) to fold imported Go struct fields into the merged sema.Info so a foreign `derive func` resolves. Gate: corpus.TestASTEngineWholeCorpusBehavioralGate runs ALL 108 manifest cases through the AST engine at the per-kind behavioral tier (transpile->RunCompile, package->RunPackage via backend.TranspilePackage, check->RunCheck via SemaCheck, doctest->RunDoctestExec) with zero failures, and guards its own scope (must span every tier). Per-check runner tests TestSemaAssertRunner/TestSemaConvertRunner + sema unit tests assert_test.go/convert_test.go + backend TestASTEngineAssignMatch*/TestASTEnginePackageBehavioralTier."
    },
    {
      "id": "US-042",
      "title": "Regenerate goldens and make AST default",
      "description": "As a maintainer, I need exact goldens regenerated and the new engine defaulted so the new path is canonical.",
      "acceptanceCriteria": [
        "The .go.expected goldens are regenerated from the new backend and the manifest exact tier is green against the new default engine",
        "The old splice engine remains available behind a flag for one release"
      ],
      "priority": 42,
      "passes": true,
      "notes": "Regenerated all 52 file-mode exact goldens FROM the AST backend (backend.Transpile) via a durable, off-by-default `-update-goldens` flag (internal/corpus/update_goldens_test.go, mirrors parser's -update-snapshots; gofmt-normalized; Output.Test for doctest sidecars per isDoctestSidecar, else Output.Go) — 42 changed (scope-aware gensym `ok`/`err1` vs `__goal_ok`, `type T int64` vs `= int64` alias, dropped `//` comments), 10 already byte-identical. Switched the 4 exact-tier tests (corpus TestTranspileRunner/TestDoctestRunner, pipeline_test TestCorpusTranspile/TestCorpusDoctest) from pipeline.Transpile to backend.Transpile — green by construction. Flipped cmd/goal default engine to engineAST (parseFlags + usage/Summary text + main_test default assertion) and routed the default package path through backend.TranspilePackage (cross-file merge + single shared prelude) instead of the per-file helper (removed transpilePackageAST). Exported pipeline.AddLineDirectives (name-based, engine-agnostic) and applied it in backend.TranspilePackage so AST-default build errors still map to .goal source (fixed TestBuildErrorMapsToGoalSource regression). Regenerated AI-KNOWLEDGE-BOOTSTRAP.md (ast default text). Splice stays behind --engine=splice and is still behaviorally exercised (RunCompile/RunDoctestExec/RunPackage via pipeline.Transpile). Whole-corpus behavioral gate (108 cases) + full suite green."
    },
    {
      "id": "US-043",
      "title": "Delete splice passes and dead scanning",
      "description": "As a maintainer, I need the obsolete splice machinery removed so there is one front-end.",
      "acceptanceCriteria": [
        "internal/pass, scan's Splice machinery, and the analyze token-scan tables subsumed by sema are deleted; grep finds no references to them",
        "Build, vet, and the full suite are green after removal"
      ],
      "priority": 43,
      "passes": true,
      "notes": "Deleted internal/pass (20 files) and the pipeline splice ENGINE (Transpile/TranspilePackage/runPasses/doctestFile/preludeFile/Passes), keeping only the engine-independent pipeline.Output/GoFile/PackageOutput types + sourcemap (AddLineDirectives). Re-pointed every remaining splice call site onto the AST backend (cmd/goalc, cmd/build-playground, cmd/goal-wasm, internal/guide, internal/typecheck, and the corpus behavioral runners behavior_runner_test/doctest_behavior_runner_test/package_runner_test) and removed the --engine=splice flag from cmd/goal (parseFlags now returns no engine; transpileAll always uses backend.TranspilePackage). grep finds NO import of internal/pass and NO call to pipeline.Transpile/TranspilePackage on the live path. GOTCHA discovered: internal/typecheck ran depth checks (on the default `goal build/check` path) THROUGH the splice engine, which passed generics + type aliases through verbatim; moving it to backend.TranspilePackage exposed two real AST parity gaps that had to be closed in-story: (1) the parser now parses generic type-parameter DECLARATIONS `type Box[T any]` (new ast.TypeSpec.TypeParams *FieldList + parser.parseTypeParams + atTypeParams disambiguation vs `[N]T`/`[]T`/`[3]T` arrays using kindAt(2) lookahead; instantiation `Box[int]` already parsed via IndexExpr) and (2) the backend emits type aliases faithfully `type T = U` (new ast.TypeSpec.Assign token.Pos, parser records it, emit.go emits `= `), so go/types sees identical types again. Regenerated 7 alias goldens (status*, defaults_refs, enum_match_result) via -update-goldens (alias `type Time int64`->`type Time = int64`) and the AI-KNOWLEDGE-BOOTSTRAP.md (--engine flag gone). SCAN/ANALYZE NOT pruned: scan.Splice/Replacement stay (internal/fix still uses them), and every analyze fact-table symbol is still consumed by check/typecheck/lsp/fix/backend — nothing was used SOLELY by the deleted passes, so there is no dead scanning code to remove at this point in the sequence (it frees up only once US-044 fix->AST and US-046 lsp->AST land). Deleted the 4 splice-only pipeline internal tests (foreign/pipeline_package/question_arity/question_refusal) and the splice half of sourcemap_test (AddLineDirectives unit test kept; the U5 end-to-end source-map proof is covered on the AST path by cmd/goal TestBuildErrorMapsToGoalSource). Whole-corpus behavioral gate + full suite (18 pkgs) green; build/vet incl. js/wasm green."
    },
    {
      "id": "US-044",
      "title": "Move goal fix onto the AST",
      "description": "As a goal developer, I need fix to operate on the AST so it shares one model with the compiler.",
      "acceptanceCriteria": [
        "internal/fix consumes the AST instead of re-lexing, and emits the same rewrites",
        "The existing fix test suite passes unchanged in observable behavior"
      ],
      "priority": 44,
      "passes": true,
      "notes": "internal/fix now drives every rule off the parsed AST: fix.File parses each fixed-point pass with parser.ParseFile and resolves name-keyed facts with sema.Resolve (FuncSignatures.Mode/T, Enums), replacing scan.Lex + analyze.Build + analyze.FuncSpans/SigAt + scan.ScanFuncs (grep confirms none remain in internal/fix). Detection is structural: result-sig reads FuncType.Results (FieldList Opening/Closing parens; all-unnamed (T,error) tuple only) and classifies ReturnStmts (walkReturns skips nested FuncLit by not descending into exprs); propagate matches consecutive AssignStmt+IfStmt (value-binding) and IfStmt.Init (init-guard) over BinaryExpr nil-guards + one valid propagation ReturnStmt (Result.Err(v) CallExpr / zero,err / Option.None SelectorExpr / nil); Option `*o`->`o` deref via StarExpr walk with escape detection; switch-over-enum walks SwitchStmt first CaseClause SelectorExpr label vs info.Enums; call-site walks ModeNone IfStmt value-binding guards. Edits stay minimal scan.Replacement byte splices applied with scan.Splice (pure string utils, NOT lexing) so untouched code/comments are byte-preserved — printing the AST would reflow. GOTCHA: the parser drops // and /* comments, so the comment-block refusal must stay a raw byte-range spanHasComment scan over the source offsets (token.Pos.Offset), not an AST check. analyze.ZeroLit is kept (a pure helper) fed by an AST-built typeDecls map. Public API (File/Change/Report/Level) unchanged; cmd/goal + internal/lsp consume it as-is. fix_test.go is byte-identical (no diff) and green; full suite (build/vet/test) green. On unparseable source fix.File no-ops the iteration (conservative contract)."
    },
    {
      "id": "US-045",
      "title": "Add goal fmt source printer",
      "description": "As a goal developer, I need goal fmt so goal source has a canonical, comment-preserving format.",
      "acceptanceCriteria": [
        "goal fmt prints goal source from the AST preserving comments, and is idempotent",
        "A test asserts fmt(fmt(src)) == fmt(src) for every corpus .goal input and that comments are retained on a sample"
      ],
      "priority": 45,
      "passes": false,
      "notes": "This is the one place comment/whitespace fidelity matters; use position-carrying nodes + comment attachment."
    },
    {
      "id": "US-046",
      "title": "Switch LSP document model to AST",
      "description": "As an editor user, I need the LSP backed by the AST so richer features become possible.",
      "acceptanceCriteria": [
        "internal/lsp diagnostics and document symbols are derived from the AST (lsp/symbols.go scanDecls token walk removed)",
        "The existing LSP test suite passes"
      ],
      "priority": 46,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-047",
      "title": "Add LSP semantic tokens",
      "description": "As an editor user, I need semantic tokens so goal constructs are highlighted by role.",
      "acceptanceCriteria": [
        "The server advertises and serves textDocument/semanticTokens classified from the AST",
        "A test asserts the token classification of a sample containing an enum, a match, and a ? expression"
      ],
      "priority": 47,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-048",
      "title": "Add LSP go-to-definition",
      "description": "As an editor user, I need go-to-definition so I can navigate goal symbols.",
      "acceptanceCriteria": [
        "textDocument/definition resolves a referenced symbol to its declaration position via the AST symbol graph",
        "A test asserts definition of a function call and an enum variant resolve to the expected positions"
      ],
      "priority": 48,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-049",
      "title": "Add LSP hover with types",
      "description": "As an editor user, I need hover so I can see a symbol's type and doc.",
      "acceptanceCriteria": [
        "textDocument/hover returns the type and any doc comment for a symbol under the cursor",
        "A test asserts hover over a Result-returning function reports its signature"
      ],
      "priority": 49,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-050",
      "title": "Add LSP rename and find references",
      "description": "As an editor user, I need rename and find-references so I can refactor goal safely.",
      "acceptanceCriteria": [
        "textDocument/references and textDocument/rename return all reference sites / workspace edits via the AST symbol graph",
        "A test asserts renaming a symbol produces edits at every reference in a sample"
      ],
      "priority": 50,
      "passes": false,
      "notes": ""
    }
  ]
}
