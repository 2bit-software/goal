# Implementation Progress — `goal ai`

## Progress Log

### T01 — Extract parser to internal/byexample
- Status: Complete
- Files: `internal/byexample/byexample.go` (new)
- Notes: Ported parse layer verbatim from build-playground; exposes raw-markdown `Feature`
  records (`DescriptionMD`/`LoweringMD`), `Source`, `SourceName`, `OutputKind`, `LockedExpected`.

### T02 — Refactor build-playground onto byexample
- Status: Complete
- Files: `cmd/build-playground/main.go` (rewritten to import byexample; keeps HTML render + verify)
- Verification: ran the ORIGINAL (git HEAD) build-playground and the refactored one against the
  same working tree — **output byte-identical** (`diff` empty). Refactor is output-preserving.
- Side finding: the committed `site/features.json` was STALE vs the working tree (already-modified
  `docs/overview.md` + the transpiler's real `__goal_` prefix, committed file had `__gop_`).
  Regenerated it; it now matches the live transpiler. The earlier "live uses `__gop_`" note was
  backwards — live is `__goal_`.

### T04/T05 — Command registry + `ai` command + `--ai` alias
- Status: Complete
- Files: `cmd/goal/main.go` (registry `guideCommands`, `topUsage`, refactored `run`, `cmdAI`)
- Notes: `run` branches `ai`/`--ai` before flag parsing (ai takes a section, not a path);
  build/run/check behavior unchanged (existing tests pass).

### T06 — Root embed package
- Status: Complete
- Files: `assets.go` (`package goal`, embeds docs/by-example.md, docs/overview.md, docs/ai)
- Notes: root package solves go:embed's no-`..` limit; no doc duplication.

### T07/T08 — Guide assembler + section access
- Status: Complete
- Files: `internal/guide/guide.go` (`Render(w, section, cmds)`, 8 fixed-order sections)
- Notes: features re-transpiled live; intentional-error features show real Transpile error;
  unknown section errors with the valid-name list. No runtime disk reads (embed only).

### T09/T10 — Diagnostics catalog + live feedback sample
- Status: Complete
- Files: `internal/guide/catalog.go` (28 codes, accurate feature+severity), `internal/guide/feedback_sample.goal`
- Notes: catalog grouped/sorted deterministically; feedback sample runs the real lexical
  checker live and prints actual diagnostics.

### T11 — Authored fragments
- Status: Complete
- Files: `docs/ai/{intro,toolchain,conventions,authoring,pointers,diagnostics}.md`, `docs/ai/starter.goal`
- Notes: sourced from old bootstrap, corrected to `__goal_`. Starter redesigned so its doctest
  targets a plain Go call (`greet("Ada")`) — the bootstrap's original `Cmd.Greet(name:...)`
  doctest would NOT compile (doctests extract the un-lowered expression). Drift caught.

### T12–T15, T17 — Drift guards + command tests
- Status: Complete
- Files: `internal/guide/guide_test.go`, `cmd/goal/ai_test.go`
- Notes: live-lowerings, catalog set-equality (scans non-test check/typecheck source, exactly
  28), section coverage, unknown-section, toolchain-lists-all-commands, ai/`--ai` behavior,
  bootstrap golden. NEGATIVE-tested: golden fails on stale file; catalog fails on undocumented
  code. Both restored.

### T16 — Regenerate bootstrap golden artifact
- Status: Complete
- Files: `AI-KNOWLEDGE-BOOTSTRAP.md` (1302 lines, generated by `goal ai`)
- Notes: header (generated + regen command) is part of the guide output, so file == output.

### T18 — Final validation
- Status: Complete
- Notes: `go test ./...` all green; `go vet ./...` clean; gofmt clean. Starter from the guide
  works end-to-end (`goal run` → hello, Ada; doctest `ok`). `site/features.json` is a gitignored
  build artifact and the refactor is output-identical (original vs refactored diff empty).
