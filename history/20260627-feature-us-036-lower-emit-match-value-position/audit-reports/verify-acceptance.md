# Verification — Acceptance Coverage (US-036)

## Verify gates (prd.json verifyCommands)
- `go build ./...` — PASS
- `go vet ./...` — PASS
- `go test ./... -count=1` — PASS (all packages green)

## Acceptance criteria

### AC1 — type-switch for enum match in statement and value position
SATISFIED. `internal/backend/emit.go` adds `enumMatch` (type-switch over the §8.1
encoding) wired into three positions:
- statement: `matchStmt` default branch routes a resolved-enum match;
- return: `returnStmt` routes `return match …`;
- var: `tryVarMatch` routes `var name T = match …` (emits `var name T` first).
Pinned by `TestASTEngineEnumMatchEncoding` (switch/case/guard-field-export/
panicking-default/`return <body>`/`name = <body>`).

### AC2 — 02-match cases + a new value-position case pass the behavioral tier
SATISFIED. New fixture `features/02-match/examples/shape_value.goal`
(+`.go.expected`, golden generated by the canonical splice engine) added; manifest
regenerated to 108 cases. `TestASTEngineEnumMatchBehavioralTier` drives all four
02-match cases plus shape_value through `backend.Transpile` + `corpus.RunCompile`
(temp-module build+vet); all pass.

## Regression
- `internal/corpus/TestGenerateCounts` updated (51 -> 52 file-mode transpile) and
  passes; the new case also runs through the existing splice transpile runner and
  behavioral tiers (corpus package green).

## Assumptions
- Exact golden-text parity for the AST engine is deferred to US-042; US-036 is
  judged by build+vet behavior (the established tier for US-032..035).
- The new fixture uses `return match` and `var T = match` (both splice-supported)
  so the canonical transpile runner stays green.
